# Supabase Configuration (Get from Supabase Dashboard)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_BUCKET=material-map

# Database Configuration
# Format: postgresql+psycopg://user:password@host:5432/database
# Get this from Supabase → Settings → Database → Connection Pool
DATABASE_URL=postgresql+psycopg://postgres:password@host:5432/postgres

# JWT Configuration (Change these for production!)
SECRET_KEY=Material-Map-Secret-Key-Change-In-Production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server Configuration
ENVIRONMENT=production
DEBUG=False
HOST=0.0.0.0
PORT=10000  # Render will override this

# CORS Configuration (for frontend domain)
CORS_ORIGINS=https://your-domain.com,https://material-map.onrender.com

# Optional: For production monitoring
# SENTRY_DSN=https://your-sentry-url
